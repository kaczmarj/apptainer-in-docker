name: Docker Image CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@v3

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build the Docker image
      run: docker build -t ghcr.io/${{ github.repository_owner }}/apptainer-in-docker:latest .

    - name: Test the Docker image (print Apptainer version)
      run: docker run --rm ghcr.io/${{ github.repository_owner }}/apptainer-in-docker:latest version

    - name: Push the Docker image
      run: docker push ghcr.io/${{ github.repository_owner }}/apptainer-in-docker:latest
